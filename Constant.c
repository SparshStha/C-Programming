// constant

#include<stdio.h>
#include<conio.h>

int main(){
	/* constant = fixed value that cannpt be altered by the program during
	its execution */
	
	const float PI = 3.1969;
	// if we put this value: PI = 123.123; then, the output won't appear
	printf ("%.2f", PI);
	return 0;
}
